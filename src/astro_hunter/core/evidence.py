"""Evidence collection and traceability.

NOT IMPLEMENTED. Placeholder; see ``docs/ARCHITECTURE.md``.

Responsibility: run the domain's checks against a signal, collect the
``Evidence`` each produces, and assemble a ``Dossier``.

Boundaries: does not interpret. Assembling evidence and drawing a conclusion
are separate steps so that the evidence survives a change of interpretation.
"""
