@AGENTS.md

# Claude Code specific instructions

Use `AGENTS.md` as the canonical shared project instructions.

For changes that touch multiple scientific stages or materially alter numerical methodology, use planning before editing and keep the change narrowly scoped.

Prefer inspecting the existing implementation over regenerating files from scratch.

Do not use auto-memory to redefine scientific methodology or overwrite repository-level scientific rules.

Local machine-specific preferences belong in `CLAUDE.local.md`, not in this file.
