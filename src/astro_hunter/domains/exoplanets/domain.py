"""Exoplanet domain - what counts as known, instrumental and interesting.

NOT IMPLEMENTED. Placeholder; see ``docs/ARCHITECTURE.md``.

This module is the whole point of D-012: everything domain-specific lives
behind this interface, so a second domain is a package rather than a fork.

Supplies: which catalogs to query and in what order, what a match means, which
instrumental windows apply, and how evidence maps to a verdict.
"""
